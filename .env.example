# Duplicate this file as .env and set one mode explicitly.

# Online mode (default): requires a valid OpenAI API key.
OPENAI_API_KEY=your_openai_api_key_here

# Offline mode for deterministic local demo/tests (no network calls).
# Set to true to force offline fallback behavior.
OPENAI_OFFLINE=false
